{% extends 'base.html.twig' %}

{% block title %}Test filter!{% endblock %}

{% block body %}
<div>
<a href="{{ path('game_search') }}">Rechercher un jeu</a>
</div>
<div>
    {% for game in games %}
        <h3>{{ game.title }}</h3>
    {% endfor %}
</div>

{% endblock %}
